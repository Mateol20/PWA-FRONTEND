Preguntas Teóricas
¿Qué diferencia hay entre == y === en JavaScript? Mostrá un ejemplo donde den resultados distintos y explicá por qué.
 - El == no tiene en cuenta el tipo de dato que es solo compara contenido por ej podemos decir
    let numero = 5 y let numero2 = "5", y al compararlos nos daria true.
 - En cambio el === si tiene en cuenta el tipo de dato y en el ejemplo anterior daria false al compararlos
    ya que se trata de un int y un string

¿Qué es la coerción de tipos? Mostrá dos ejemplos de coerción implícita y uno de coerción explícita.
 - La coercion de tipos es el proceso automatico de transformar un tipo de dato a otro de manera implicita o explicita
    Por ejemplo yo puedo decir let numero = 5 y let numero2 = "5" hacer numero + numero2 y esto devolveria 10 automaticamente
    el compilador de manera implicita decide tranformar el string en un numero
    distinto seria si yo explicitamente le diria let numero2 = Number("5") ya que explicitamente le estoy pidiendo que lo convierta
    a un valor numerico.

¿Qué resultado devuelve una expresión como false || "hola" o "hola" && 0? Explicá el rol de los operadores lógicos en esos casos.

¿Cuáles son las principales estructuras de control de flujo en JavaScript? Mencioná if, else, switch, y explicá brevemente en qué casos conviene usar cada uno.
 -If evalua una operacion de manera booleana
 -else es la salida de la evaluacion del if
 -switch es una multiple evaluacion donde damos opciones segun lo evaluado

¿Para qué sirve el operador ternario (condición ? a : b)? Reescribí un if simple con este operador.
-El ternario sirve para evaluar algo con una sola opcion de salida
-If(evaluacion){(accion)}else{(accion)}

¿Cómo funcionan los bucles for y while en JavaScript? Mostrá un ejemplo de cada uno.
-for evalua caso por caso hasta cumplir una condicion generalmenete un contador
    for(let i = 0 ; i<10; i++){}
-while evalua constantemente mientras la conidcion establecida se cumpla si esta conidicion se rompe el ciclo se corta
while(estudiante = "alumno"){
    materia = desaprobada
    if(materia == aprobada){
        estudiante = recibido
    }
}

¿Qué hacen break y continue dentro de un bucle? ¿Qué efecto tienen en la ejecución del código?
-break corta la ejecucion de la compilacion, generalmente se usa para salir de un bucle
-continue es una operacion dentro de los bucle que indica saltarse el ciclo actual y pasar al siguiente.